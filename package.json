{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "spago": "^0.21.0",
    "purescript": "^0.15.15",
    "purescript-psa": "^0.5.1"
  }
}
