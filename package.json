{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "spago": "^0.20.9",
    "purescript": "^0.15.0",
    "purescript-psa": "^0.5.1"
  }
}
